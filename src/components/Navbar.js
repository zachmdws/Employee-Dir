import React from "react";
import "../styles/Navbar.css";

function Navbar() { 
    return (
        <nav class="navbar">
            <a class="navbar-brand">
                Employee Directory
            </a>
        </nav>
    );
}

export default Navbar;